# NPM Discovery Configuration
# Copy this file to .env and fill in your API keys

# Libraries.io API Key (Required)
# Get yours at: https://libraries.io/account
LIBRARIES_IO_API_KEY=your_api_key_here

# Optional Configuration
# NPM Registry URL (default: https://registry.npmjs.org)
# NPM_REGISTRY_URL=https://registry.npmjs.org

# unpkg CDN URL (default: https://unpkg.com)
# UNPKG_URL=https://unpkg.com

# Cache TTL in days (default: 7)
# CACHE_TTL_DAYS=7

# Maximum concurrent HTTP requests (default: 40)
# MAX_CONCURRENT_REQUESTS=40

# HTTP request timeout in seconds (default: 30)
# REQUEST_TIMEOUT=30

# Download directory (default: ~/npm_packages)
# DOWNLOAD_DIR=~/npm_packages

